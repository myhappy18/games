apiVersion: v1
kind: Service
metadata:
  name: busy
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "busy"
    port: 8080
  selector:
    app: busy
